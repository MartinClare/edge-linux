# Shared app dependencies. Install a PyTorch build *before* this file — see requirements.txt
# for NVIDIA DGX vs Rockchip. (Ultralytics expects either CPU/GPU torch already present or will pull torch.)

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
pydantic>=2.0.0
python-multipart>=0.0.6
requests>=2.31.0
ultralytics>=8.0.0
